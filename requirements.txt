# Salary & Goals Calculator — v1.0 RC
# Runtime dependencies
# Install with: pip install -r requirements.txt

pillow>=10.0.0
reportlab>=4.0.0

# Build dependency (only needed to compile the .exe — not needed to run the app)
# pyinstaller>=6.0.0
